/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "tailwindui.com",
      "prospera.or.id",
      "images.unsplash.com",
      "camo.githubusercontent.com",
    ],
  },
};

module.exports = nextConfig;
